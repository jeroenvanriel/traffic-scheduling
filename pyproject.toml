[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "traffic_scheduling"
version = "0.1.0"
description = "Autonomous Intersection Coordination"
authors = [{name="Jeroen van Riel"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
